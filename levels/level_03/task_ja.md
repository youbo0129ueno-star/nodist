`head` から `next` をたどり、リストの値を順に出力してください。

入力からリストを作る処理はテンプレートに用意されています。`print_list` 関数の中で、`current` を使って先頭から末尾まで走査してください。

## 入力

```text
n
a[0] a[1] ... a[n-1]
```

## 出力

```text
a[0] a[1] ... a[n-1]
```
