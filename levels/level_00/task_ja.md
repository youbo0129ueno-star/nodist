`struct Node` を定義し、1個のノードに入力値を入れて出力してください。

ノードは次の2つのメンバを持つものとします。

- `int key`
- `struct Node *next`

入力された整数を `key` に入れ、`next` には `NULL` を入れてください。出力は `key` と、`next` が `NULL` かどうかを表す文字列です。

## 入力

```text
x
```

## 出力

```text
x NULL
```
