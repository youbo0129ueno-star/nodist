リスト操作の計算量を答えてください。

入力として操作名が1つ与えられます。その操作の最悪計算量を `O(1)` または `O(n)` で出力してください。

対象の操作は次の5つです。

- `search`
- `insert_after_known_node`
- `delete_known_node_doubly`
- `delete_by_key`
- `minimum`

## 入力

```text
operation
```

## 出力

```text
O(1)
```

または

```text
O(n)
```
