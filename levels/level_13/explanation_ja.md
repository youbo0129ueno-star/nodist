## 方針

リストには大小関係にもとづく構造が入っていません。そのため、最大値や最小値を求めるには先頭から末尾まで調べる必要があります。

## ポイント

最小値を探す場合、まず先頭ノードを仮の最小ノードにします。

```c
struct Node *best = head;
```

その後、全ノードを見ながら、より小さい値があれば `best` を更新します。最大値も同じ考え方です。

## 注意点

返り値を `int` にすると、値は分かっても「どのノードか」は分かりません。後の挿入や削除につなげるには、ノードへのポインタを返せることが重要です。
