リストから、指定された値を持つ最初のノードを1個削除してください。

`delete_value` 関数を完成させます。削除後の新しい `head` を返してください。対象が見つからない場合、リストはそのままです。

## 入力

```text
n target
a[0] a[1] ... a[n-1]
```

## 出力

削除後のリストを出力します。空になった場合は `EMPTY` と出力します。
