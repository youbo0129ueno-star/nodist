双方向リスト用の `struct DNode` を定義してください。

ノードは次の3つのメンバを持ちます。

- `struct DNode *prev`
- `int key`
- `struct DNode *next`

入力された整数を `key` に入れ、`prev` と `next` はどちらも `NULL` にしてください。

## 入力

```text
x
```

## 出力

```text
x NULL NULL
```
