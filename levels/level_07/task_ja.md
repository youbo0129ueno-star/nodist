探索処理を関数として切り出し、見つかったノードへのポインタを返してください。

`list_search` 関数を完成させます。見つかった場合はそのノードへのポインタ、見つからない場合は `NULL` を返します。

## 入力

```text
n target
a[0] a[1] ... a[n-1]
```

## 出力

見つかった場合:

```text
FOUND value
```

見つからない場合:

```text
NOT FOUND
```
