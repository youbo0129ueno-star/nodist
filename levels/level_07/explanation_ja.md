## 方針

このレベルでは、探索結果を真偽値ではなく「ノードへのポインタ」として返します。

## ポイント

見つかったノードそのものを後で使いたい場合、`1` や `0` では情報が足りません。そこで、見つかった場所を表すポインタを返します。

```c
if (current->key == target) {
    return current;
}
```

見つからなかった場合は、存在しないことを表すために `NULL` を返します。

## 注意点

関数の中で新しいノードを作る必要はありません。リスト中にすでに存在するノードを指すポインタを返します。
