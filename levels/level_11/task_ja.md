双方向リストで、指定された位置の直後に新しいノードを挿入してください。

`insert_after` 関数を完成させます。`p` の次に新ノード `x` を入れます。`p` が `NULL` の場合は先頭挿入として扱います。

入力では、リストの値と「何番目のノードの後ろに挿入するか」が与えられます。位置は `0-indexed` です。`-1` の場合は先頭に挿入します。

## 入力

```text
n pos value
a[0] a[1] ... a[n-1]
```

## 出力

挿入後のリストを先頭から出力します。
