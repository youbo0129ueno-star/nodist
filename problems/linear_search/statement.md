# linear_search

配列 `A` から値 `x` を線形探索してください。

`x` が見つかった場合は、最初に見つかった位置を `0-indexed` で出力します。見つからない場合は `-1` を出力します。

## 入力

```text
n x
a[0] a[1] ... a[n-1]
```

## 出力

```text
位置 または -1
```

## 制約

- `1 <= n <= 1000`
- 配列の要素と `x` は整数

## 入力例

```text
5 3
1 2 3 4 5
```

## 出力例

```text
2
```
