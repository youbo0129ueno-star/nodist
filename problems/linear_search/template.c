#include <stdio.h>

int main(void) {
    int n, x;
    int a[1000];

    scanf("%d %d", &n, &x);
    for (int i = 0; i < n; i++) {
        scanf("%d", &a[i]);
    }

    /* TODO: x を探して、位置または -1 を出力する */

    return 0;
}
